PROMPT1 = f"""
Ты выполняешь точное извлечение данных из сканированного банковского документа.

Тебе переданы:
1. Все страницы одного текущего документа в виде изображений.
2. OUTPUT_SCHEMA — словарь соответствия полей.

ВАЖНО:
В OUTPUT_SCHEMA:
- КЛЮЧ — техническое название поля на английском языке.
- ЗНАЧЕНИЕ — описание этого поля на русском языке.

Русское описание используй ТОЛЬКО для поиска соответствующего значения
в документе.

В итоговом JSON в качестве ключей используй ИСКЛЮЧИТЕЛЬНО
английские ключи из OUTPUT_SCHEMA.

Запрещено:
- заменять английский ключ его русским описанием;
- переводить ключи;
- изменять написание ключей;
- добавлять новые ключи;
- удалять ключи.

Пример:

OUTPUT_SCHEMA:
{{
  "DepartmentCode": "Код подразделения",
  "BorrowerName": "Наименование заемщика"
}}

Правильный ответ:
{{
  "DepartmentCode": "123",
  "BorrowerName": "ООО Ромашка"
}}

Неправильный ответ:
{{
  "Код подразделения": "123",
  "Наименование заемщика": "ООО Ромашка"
}}

Если значение поля в документе не найдено, верни для него пустую строку "".

Верни ВСЕ и ТОЛЬКО ключи из OUTPUT_SCHEMA.

Для поля RateBasisValue%n верни пустую строку, если в RateBasis%n
указана ключевая ставка Банка России.

Для поля ReturnDelayPeriod верни только итоговую сумму.

Не используй Markdown.
Не используй ```json.
Не добавляй объяснений.
Не добавляй текст до или после JSON.

OUTPUT_SCHEMA:
{json.dumps(
    output_schema,
    ensure_ascii=False,
    indent=2
)}
"""
